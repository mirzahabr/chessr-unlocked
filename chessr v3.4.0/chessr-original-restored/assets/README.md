# assets/

Stream Mode's stylesheet, loaded by `../stream.html`. Filename carries a content-derived
hash suffix that changes on every build — read the current name from `stream.html`'s
`<link href>` rather than hard-coding it.
